# Archived scripts

Kept for reference, not part of any current workflow.

- `local.bat` — mirrored `posts/` into `hugo/content/posts` and ran `hugo server`.
  It never ran `meta.py` or `slug.py`, so posts previewed without their front
  matter: no title, no date, no cover. Reviewing the deployed site directly is
  the intended workflow instead.
